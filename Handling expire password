#Powershell

#조회
Get-LocalUser |
Select-Object Name, Enabled, LastLogon, PasswordLastSet, PasswordExpires, AccountExpires, PasswordRequired, UserMayChangePassword |
Format-Table -AutoSize


#패스워드 변경
$User = "User ID"
$Password = ConvertTo-SecureString "User 변경할 PW" -AsPlainText -Force
Set-LocalUser -Name $User -Password $Password
Set-LocalUser -Name $User -PasswordNeverExpires $false
net user $User /passwordchg:yes
